#include <iostream>
#include <vector>
#include <map>

using namespace std;

int max_attack;


void attack(){

}


int main(){
    max_attack = 0;

    int block, num_mon, num_tower, range, ele;
    vector<int> monster_HP(num_mon);
    vector<int> tower(num_tower);
    vector<int> ranges(num_tower);

    //input hp of each monstsers for # num_mon
    for (int i=0; i<num_mon; i++){
        cin >> ele;
        
    }

}